#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) 

{        // a et b deux nombre entier

	int a,b,s ;
	   // la valeur de a et b
	printf("la valeur de a :");
	scanf("%d",&a);
	printf("la valeur de b :");
	scanf("%d",&b);
	
	        // la somme deux nombre
	        
	 s = a + b ;
	 printf(" la somme deux nombre est : %d ",s);
	return 0;
}
